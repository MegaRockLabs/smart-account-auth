[package]
name        = "saa-proto-core"
description = "Procedural macros of saa-schema for wasm"
edition     = { workspace = true }
authors     = { workspace = true }
version     = "0.25.1"
license     = { workspace = true }
repository  = { workspace = true }

[lib]
proc-macro = true

[dependencies.proc-macro2]
version = "1"

[dependencies.quote]
version = "1"

[dependencies.syn]
version = "1"
features = [
    "full",
    "printing",
    "extra-traits",
]
[package]
name        = "saa-auth"
description = "Custom credentials built on top of curves and their verification logic for smart account auth"
edition     = { workspace = true }
authors     = { workspace = true }
license     = { workspace = true }
repository  = { workspace = true }
version     = { workspace = true }

[dependencies]
hex                 = { version = "0.4.3", optional = true, default-features = false }
serde_json          = { workspace = true,  optional = true }
serde               = { workspace = true }
schemars            = { workspace = true }

saa-crypto          = { workspace = true }
saa-common          = { workspace = true }
saa-schema          = { workspace = true }

# ethabi              = { version = "0.26.0", default-features = false, features = ["serde"] }
primitive-types      = { version = "0.12.2", optional = true, default-features = false, features = ["serde"] }

[features]
default             	=   ["std"]
std                 	=   ["hex?/std"]
utils               	=   []
replay                  =   []


native              	=   ["saa-crypto/native"  ] 
cosmwasm            	=   ["saa-crypto/cosmwasm"]

eth_personal        	=   ["saa-crypto/ethereum", "saa-common/eth_personal", "dep:hex"]

eth_typed_data      	=   ["saa-crypto/ethereum", "saa-common/eth_typed_data", "dep:primitive-types",  "dep:serde_json", "dep:hex"]
#eth_typed_data_cache    =   ["eth_typed_data"]  
eth_typed_data_replay   =   ["eth_typed_data"]  

cosmos_arb          	=   ["saa-crypto/cosmos_arb"]
cosmos_arb_addr     	=   ["saa-common/cosmos_arb"]



